# drei/leva pull optional expo peers that clash with newer react resolution;
# runtime react comes from WordPress itself (wp-scripts externals), so legacy
# peer resolution is safe here.
legacy-peer-deps=true
